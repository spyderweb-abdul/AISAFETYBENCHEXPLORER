# AISafetyBenchExplorer — Python Dependencies
# =============================================
# Install with: pip install -r requirements_doi_pipeline.txt
#
# Optional extras are commented out; uncomment what you need.

# ---------------------------------------------------------------------------
# Core data validation and HTTP
# ---------------------------------------------------------------------------
pydantic>=2.5.0
requests>=2.31.0
httpx>=0.26.0            # Async HTTP client (used in resolver and batch mode)
python-dateutil>=2.8.2
tenacity>=8.2.3          # Retry logic for flaky API calls

# ---------------------------------------------------------------------------
# Scholarly API clients
# ---------------------------------------------------------------------------
arxiv>=2.1.0             # arXiv API wrapper
feedparser>=6.0.10       # arXiv RSS/Atom fallback parser
# semanticscholar>=0.8.0  # Optional: official S2 client (we use REST directly)
# unpywall>=0.1.0         # Optional: Unpaywall wrapper (we use REST directly)

# ---------------------------------------------------------------------------
# LLM / structured-output backends
# ---------------------------------------------------------------------------
instructor>=1.2.0        # Structured LLM output (Pydantic-enforced)
openai>=1.30.0           # OpenAI API client (also used for Ollama-OpenAI compat)
# anthropic>=0.25.0      # Optional: Anthropic Claude backend
# ollama>=0.2.0          # Optional: local Ollama Python client

# ---------------------------------------------------------------------------
# PDF parsing (choose at least pymupdf; others are optional upgrades)
# ---------------------------------------------------------------------------
pymupdf>=1.23.0          # Basic PDF → text (always required as fallback)
# marker-pdf[full]>=0.2.0  # Optional: fast PDF → Markdown (GPU-accelerated)
# nougat-ocr>=0.1.17       # Optional: best accuracy for math-heavy papers

# ------------------------------